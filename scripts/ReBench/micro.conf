# this run definition will be chosen if no parameters are given to rebench
default_experiment: all
default_data_file: 'micro.csv'

# a set of suites with different benchmarks and possibly different settings
benchmark_suites:
    Alone:
        gauge_adapter: PlainSecondsLog
        command: |-
            eval "%(benchmark)s"
        input_sizes: [1]
        invocations: 30
        benchmarks:
            - "|bencher| bencher := BencherMp new. bencher useMicrodownBench. bencher benchNoInstrumentation"
    AllocationRate:
        gauge_adapter: PlainSecondsLog
        command: |-
            eval "%(benchmark)s"
        input_sizes: [1]
        invocations: 30
        benchmarks:
            - "|bencher| bencher := BencherMp new. bencher useMicrodownBench; useAllocationRateProfiler. bencher benchExecution"
    MethodCounter:
        gauge_adapter: PlainSecondsLog
        command: |-
            eval "%(benchmark)s"
        input_sizes: [1]
        invocations: 30
        benchmarks:
            - "|bencher| bencher := BencherMp new. bencher useMicrodownBench; useMethodCounterProfiler. bencher benchExecution"
    NoActionAllMethods:
        gauge_adapter: PlainSecondsLog
        command: |-
            eval "%(benchmark)s"
        input_sizes: [1]
        invocations: 30
        benchmarks:
            - "|bencher| bencher := BencherMp new. bencher useMicrodownBench; useNoActionAllMethodsProfiler. bencher benchExecution"
    NoActionAllocatorMethods:
        gauge_adapter: PlainSecondsLog
        command: |-
            eval "%(benchmark)s"
        input_sizes: [1]
        invocations: 30
        benchmarks:
            - "|bencher| bencher := BencherMp new. bencher useMicrodownBench; useNoActionAllocatorMethodsProfiler. bencher benchExecution"
#########
#########
    AllocationRateRWI:
        gauge_adapter: PlainSecondsLog
        command: |-
            eval "%(benchmark)s"
        input_sizes: [1]
        invocations: 30
        benchmarks:
            - "|bencher| bencher := BencherRwi new. bencher useMicrodownBench; useAllocationRateProfiler. bencher benchExecution"
    MethodCounterRWI:
        gauge_adapter: PlainSecondsLog
        command: |-
            eval "%(benchmark)s"
        input_sizes: [1]
        invocations: 30
        benchmarks:
            - "|bencher| bencher := BencherRwi new. bencher useMicrodownBench; useMethodCounterProfiler. bencher benchExecution"
    NoActionAllMethodsRWI:
        gauge_adapter: PlainSecondsLog
        command: |-
            eval "%(benchmark)s"
        input_sizes: [1]
        invocations: 30
        benchmarks:
            - "|bencher| bencher := BencherRwi new. bencher useMicrodownBench; useNoActionAllMethodsProfiler. bencher benchExecution"
    NoActionAllocatorMethodsRWI:
        gauge_adapter: PlainSecondsLog
        command: |-
            eval "%(benchmark)s"
        input_sizes: [1]
        invocations: 30
        benchmarks:
            - "|bencher| bencher := BencherRwi new. bencher useMicrodownBench; useNoActionAllocatorMethodsProfiler. bencher benchExecution"
#######
#######
    AllocationRateNoSafe:
        gauge_adapter: PlainSecondsLog
        command: |-
            eval "%(benchmark)s"
        input_sizes: [1]
        invocations: 30
        benchmarks:
            - "|bencher| bencher := BencherMp new. bencher useMicrodownBench; useAllocationRateNoSafeProfiler. bencher benchExecution"
    MethodCounterNoSafe:
        gauge_adapter: PlainSecondsLog
        command: |-
            eval "%(benchmark)s"
        input_sizes: [1]
        invocations: 30
        benchmarks:
            - "|bencher| bencher := BencherMp new. bencher useMicrodownBench; useMethodCounterNoSafeProfiler. bencher benchExecution"
    NoActionAllMethodsNoSafe:
        gauge_adapter: PlainSecondsLog
        command: |-
            eval "%(benchmark)s"
        input_sizes: [1]
        invocations: 30
        benchmarks:
            - "|bencher| bencher := BencherMp new. bencher useMicrodownBench; useNoActionAllMethodsNoSafeProfiler. bencher benchExecution"
    NoActionAllocatorMethodsNoSafe:
        gauge_adapter: PlainSecondsLog
        command: |-
            eval "%(benchmark)s"
        input_sizes: [1]
        invocations: 30
        benchmarks:
            - "|bencher| bencher := BencherMp new. bencher useMicrodownBench; useNoActionAllocatorMethodsNoSafeProfiler. bencher benchExecution"
##
##
# a set of executables for the benchmark execution
executors:
    ExecutorMicro:
        path: "."
        executable: "reBenchRunner.sh"
        args: "./baseimage/pharo-vm/pharo --headless ./micro/micro.image --no-default-preferences"


# combining benchmark suites and executions
experiments:
    Micro:
        suites:
          - Alone
          #
          - AllocationRate
          - MethodCounter
          - NoActionAllocatorMethods
          - NoActionAllMethods
          #
          - AllocationRateRWI
          - MethodCounterRWI
          - NoActionAllMethodsRWI
          - NoActionAllocatorMethodsRWI
          #
          - AllocationRateNoSafe
          - MethodCounterNoSafe
          - NoActionAllMethodsNoSafe
          - NoActionAllocatorMethodsNoSafe
        executions:
          - ExecutorMicro